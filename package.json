{
  "name": "snap2cal",
  "version": "1.0.0",
  "description": "Snap2Cal - Convert photos to calendar events",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "build": "rm -rf www && mkdir -p www && cp index.html landing.html admin.html faq.html privacy.html terms.html analytics.js email-capture.js apple-touch-icon.png www/ && cp -r Snap2CalLogos www/",
    "cap:sync": "npm run build && npx cap sync",
    "cap:open:ios": "npx cap open ios",
    "prepare": "husky"
  },
  "dependencies": {
    "@capacitor/app": "^8.0.0",
    "@capacitor/browser": "^8.0.0",
    "@capacitor/cli": "^8.0.2",
    "@capacitor/core": "^8.0.2",
    "@capacitor/ios": "^8.0.2",
    "@supabase/supabase-js": "^2.47.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "jest": "^30.2.0"
  }
}
